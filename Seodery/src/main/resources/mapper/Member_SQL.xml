<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">

<select id="login" parameterType="hashmap" resultType="hashmap">
	SELECT MBER_NUM, ID, NM
	FROM UR
	WHERE WD_DATE IS NULL
		AND ID = #{id}
		AND PW = #{psw}
</select>

<insert id="join" parameterType="hashmap">
	INSERT INTO UR (MBER_NUM, ID, PW, NM, GENDER, BIRTH,PHONE, EMAIL, ZCODE, ADDRESS, DTLADDRESS)
	VALUES (
		MBER_SEQ.NEXTVAL,
		#{id},
		#{pw},
		#{name},
		#{gender},
		#{birth},
		#{phone},
		#{email},
		#{zipcode},
		#{address},
		#{address_detail}
	)
</insert>

	
	<!-- 아이디 중복 검사 -->
		<select id="check_id" parameterType="String" resultType="int">
		select count(*) from member where id = #{id}
	</select>
	
	<!-- 이메일 중복 검사 -->
	<select id="check_email" parameterType="String" resultType="int">
		select count(*) from member where email = #{email}
	</select>
	
	<!-- 회원 가입 -->
	<insert id="join_member" parameterType="member">
		insert into member values(#{id}, #{pw}, #{email}, now(), now(), 'false', #{approval_key})
	</insert>
	

</mapper>